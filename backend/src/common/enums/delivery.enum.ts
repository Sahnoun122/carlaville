export enum DeliveryType {
  PICKUP = 'pickup',
  RETURN = 'return',
}

export enum DeliveryStatus {
  PENDING = 'pending',
  ASSIGNED = 'assigned',
  ON_THE_WAY = 'on-the-way',
  ARRIVED = 'arrived',
  CONFIRMED = 'confirmed',
  FAILED = 'failed',
  CANCELLED = 'cancelled',
}
